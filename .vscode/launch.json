{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "DFFRNT AI Assistant (Local)",
            "type": "debugpy",
            "request": "launch",
            "module": "dffrnt_assistant.api.app",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "preLaunchTask": "Setup: All prerequisites",
            "console": "integratedTerminal"
        }
    ]
}
